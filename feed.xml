<?xml version="1.0" encoding="utf-8"?><feed xmlns="http://www.w3.org/2005/Atom" xml:lang="en"><generator uri="https://jekyllrb.com/" version="4.3.3">Jekyll</generator><link href="https://logb-research.github.io/feed.xml" rel="self" type="application/atom+xml"/><link href="https://logb-research.github.io/" rel="alternate" type="text/html" hreflang="en"/><updated>2024-06-14T12:20:50+00:00</updated><id>https://logb-research.github.io/feed.xml</id><title type="html">blank</title><subtitle># A simple, whitespace theme for academics. Based on [*folio](https://github.com/bogoli/-folio) design. </subtitle><entry><title type="html">Kernel Trick I - Deep Convolutional Representations in RKHS</title><link href="https://logb-research.github.io/blog/2024/ckn/" rel="alternate" type="text/html" title="Kernel Trick I - Deep Convolutional Representations in RKHS"/><published>2024-04-22T00:00:00+00:00</published><updated>2024-04-22T00:00:00+00:00</updated><id>https://logb-research.github.io/blog/2024/ckn</id><content type="html" xml:base="https://logb-research.github.io/blog/2024/ckn/"><![CDATA[<h2 id="goal-️">Goal 🧙‍♂️</h2> <blockquote> <p>Fear not, those who delved into the maths of the kernel trick, for its advent in deep learning is coming.</p> </blockquote> <p>In this blog post, we focus on the Convolutional Kernel Network (CKN) architecture introduced in <d-cite key="mairal2016endtoend"></d-cite> and present its guiding principles and main components. The CKN opened a new line of research in deep learning by demonstrating the benefits of the kernel trick for deep convolutional representations. For a more complete picture, we invite the reader to refer to the original paper <d-cite key="mairal2016endtoend"></d-cite>.</p> <h2 id="kernel-trick-️">Kernel Trick 🧙‍♂️</h2> <p>We briefly recall the definition of the kernel trick. Let \(\mathcal{H}\) be a high-dimensional feature space and \(\varphi\colon \mathcal{X} \to \mathcal{H}\) a mapping. In the situation where inputs are first mapped to \(\mathcal{H}\) with \(\varphi\) and then compared using the inner product \(\langle \varphi(x), \varphi(x')\rangle_{\mathcal{H}}\), the kernel trick enables us to directly evaluate such pairwise comparison by kernel evaluation, i.e.,</p> \[\begin{equation*} K(\mathbf{x}, \mathbf{x}') = \langle \varphi(\mathbf{x}), \varphi(\mathbf{x}')\rangle_{\mathcal{H}} \end{equation*}\] <p>For the interested reader, more details can be found below or in <d-cite key="scholkopf2000kerneltrick"></d-cite>. In addition, the <a href="https://mva-kernel-methods.github.io/course-2023-2024/static_files/materials/slides.pdf">slides</a> of <a href="https://lear.inrialpes.fr/people/mairal/">Julien Mairal</a> and <a href="https://jpvert.github.io/">Jean-Philippe Vert</a> contain all one needs to know about kernel methods.</p> <details><summary>:mag_right: More details on Kernels</summary> <p>Let us consider a set \(\mathcal{X}\), a p.d. kernel \(K : \mathcal{X}\times\mathcal{X} \to \mathbb{R}\), its associated RKHS \(\mathcal{H}\) and its associated mapping fonction \(\varphi : \mathcal{X} \to \mathbb R\), i.e. the function such that \(\forall \mathbf{x},\mathbf{x}',~K(x,x') = \langle \varphi(\mathbf{x}),\varphi(\mathbf{x}') \rangle_{\mathcal{H}}\).</p> <p>In the rest of this post, we’ll be talking about CKNs, which use a particular type of kernel, namely dots product kernels. Let us consider some function \(\kappa : \mathbb{R}\to\mathbb{R}\). A dot product kernel is a p.d. kernel \(K : \mathcal{X}\times\mathcal{X} \to \mathbb{R}\) defined as \(\forall \mathbf{x},\mathbf{x}' \in \mathcal{X},~ K(\mathbf{x},\mathbf{x}') = \kappa(\langle \mathbf{x},\mathbf{x}'\rangle).\) We also define homogeneous dot product kernels as \(\forall \mathbf{x},\mathbf{x}' \in \mathcal{X},~ K(\mathbf{x},\mathbf{x}') = \lVert \mathbf{x} \rVert\lVert \mathbf{x}'\rVert \kappa \left(\left\langle \frac{\mathbf{x}}{\lVert \mathbf{x} \rVert},\frac{\mathbf{x}'}{\lVert \mathbf{x}'\rVert}\right\rangle\right).\)</p> <p>If \(\kappa\) has some regular properties (see <d-cite key="mairal2016endtoend"></d-cite>), we can use homogeneous dot product kernels to represent local image neighborhoods in \(\mathcal{H}\).</p> <p>A lot of these kernels exist, see <d-cite key="mairal2016endtoend"></d-cite> for details. These include the \textit{exponential} dot product kernel \(K_{\exp}(\mathbf{x},\mathbf{x}') = \text{e}^{\beta(\langle \mathbf{x},\mathbf{x}'\rangle -1)} = \text{e}^{-\frac{\beta}{2}\lVert \mathbf{x}-\mathbf{x}' \rVert_2^2}\), where \(\beta &gt; 0\). When \(\displaystyle\beta = \frac{1}{\sigma^2}\), we recover the well-known Gaussian Kernel. By the way, the arguments of the chosen kernels can be learned during model training!</p> </details> <p>A direct and very popular application of the kernel trick is SVM. It allows us to map data points from an initial space to a more abstract RKHS. In this way, we can move into spaces in which the data is represented in a linearly separable way (by a hyperplane). Here’s a short animation showing how the kernel trick can be used to map data initially contained in a 2D plane, to a larger space where they can be separated by a hyperplane.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog_ckn/kernel_trick_svm_hq_loop-480.webp 480w,/assets/img/blog_ckn/kernel_trick_svm_hq_loop-800.webp 800w,/assets/img/blog_ckn/kernel_trick_svm_hq_loop-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog_ckn/kernel_trick_svm_hq_loop.gif" class="img-fluid rounded z-depth-0" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>Of course, kernel trick can be used for much more than that, and in this post, we propose to show you how you can play with convolutional representations using kernel trick.</p> <h2 id="convolutional-kernel-network-in-depth-️">Convolutional Kernel Network in-depth 🧙‍♂️</h2> <h3 id="mathematical-insights-and-computability">Mathematical insights and computability</h3> <p>Kernel trick here, kernel trick there… But can we really compute this kernel trick in practice ?</p> <p>Let us define mathematically an image as a mapping \(I_0 : \Omega_0 \to \mathbb{R}^{3}\) (as an image has \(3\) color channels). If \(\mathbf{x}\) and \(\mathbf{x}'\) are two patches extracted from \(I_0\) we can get \(\varphi_1(\mathbf{x})\) and \(\varphi_1(\mathbf{x}')\), their representation in \(\mathcal{H}\), thanks to the kernel trick ! But, \(\mathcal{H}\) is an <strong><em>infinite</em></strong> dimensional space, so how to deal with it in practice? :dizzy_face:</p> <p>No panic, our friend Nyström is here to help! Nyström’s method aims to approximate \(\varphi_1(\mathbf{x})\) and \(\varphi_1(\mathbf{x}')\) by their projection \(\psi_1(\mathbf{x})\) and \(\psi_1(\mathbf{x}')\) onto a <strong><em>finite</em></strong> dimensional subspace \(\mathcal{F}\) (see the figure below).</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog_ckn/Nystrom-480.webp 480w,/assets/img/blog_ckn/Nystrom-800.webp 800w,/assets/img/blog_ckn/Nystrom-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog_ckn/Nystrom.png" class="img-fluid rounded z-depth-0" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The subspace \(\mathcal{F}\) is defined as \(\mathcal{F} = \text{span}(z_1,\dots,z_p)\), where \((z_i)_{i\in\{1\dots p\}}\) are anchor points, of unit-norm. \(Z = \{z_1,\dots,z_p\}\) are the parameters of the layer in the sense that optimizing the layer means optimizing \(\mathcal{F}\). The subspace \(\mathcal{F}\) can be optimized in both a <em>supervised</em> (with backpropagation rules) or an <em>unsupervised</em> way (by minimzing projection residuals with spherical KMeans), see <d-cite key="mairal2016endtoend"></d-cite>.</p> <h3 id="the-convolutional-kernel-network">The Convolutional Kernel Network</h3> <p>We can therefore construct our Convolutional Kernel Layer in three steps.</p> <ul> <li>We extract patches \(\mathbf{x}\) from the image \(I_0\). <details> <summary>Code</summary> <pre><code class="language-python">def extract_2d_patches(self, x):
      h, w = self.filter_size
      batch_size, C, _, _ = x.shape
      unfolded_x = np.lib.stride_tricks.sliding_window_view(x, (batch_size, C, h, w))
      unfolded_x = unfolded_x.reshape(-1, self.patch_dim)
      return unfolded_x
  
  def sample_patches(self, x_in, n_sampling_patches=1000):
      patches = self.extract_2d_patches(x_in)
      n_sampling_patches = min(patches.shape[0], n_sampling_patches)
      patches = patches[:n_sampling_patches]
      return patches</code></pre></details> </li> <li>We normalize and convolve them as \(\lVert \mathbf{x} \rVert \kappa \left( Z^\top \displaystyle\frac{\mathbf{x}}{||\mathbf{x}||} \right)\) and compute the approximation as \(\psi(x) = \lVert \mathbf{x} \rVert\kappa\left(Z^\top Z\right)^{-1/2}\kappa\left(Z^\top \displaystyle\frac{\mathbf{x}}{||\mathbf{x}||}\right)\) by applying the linear transform \(\kappa\left(Z^\top Z\right)^{-1/2}\) at every pixel location, <details> <summary>Code</summary> <pre><code class="language-python">def conv_layer(self, x_in):
      patch_norm = np.sqrt(np.clip(conv2d_scipy(x_in**2, self.ones, bias=None,
              stride=1, padding=self.padding, dilation=self.dilation,
              groups=self.groups), a_min=EPS, a_max=None))
      x_out = conv2d_scipy(x_in, self.weight, self.bias, (1,1),
                      self.padding, self.dilation, self.groups)
      x_out = x_out / np.clip(patch_norm, a_min=EPS, a_max=None)
      x_out = patch_norm * self.kappa(x_out)
      return x_out
  
  def mult_layer(self, x_in, lintrans):
      batch_size, in_c, H, W = x_in.shape
      x_out = np.matmul(
          np.tile(lintrans, (batch_size, 1, 1)), 
          x_in.reshape(batch_size, in_c, -1))
      return x_out.reshape(batch_size, in_c, H, W)</code></pre></details> </li> <li> <p>We compute the pooling operations. Note that gaussian linear pooling is defined as</p> \[\begin{equation*} \displaystyle I_1(x) = \int_{\mathbf{x}'\in\Omega_0} M_1(x') \text{e}^{-\beta\lVert \mathbf{x}-\mathbf{x}'\rVert_2^2}\text{d}\mathbf{x}' \end{equation*}\] <p>where \(M_1\) is the “feature map” after the second point operation. That is why, we can interpret the pooling operation as a “convolution” operation. </p> <details> <summary>Code</summary> <pre><code class="language-python">def pool_layer(self, x_in):
      if self.subsampling &lt;= 1:
          return x_in
      x_out = conv2d_scipy(x_in, self.pooling_filter, bias=None, 
          stride=self.subsampling, padding=self.subsampling, 
          groups=self.out_channels)
      return x_out</code></pre></details> </li> </ul> <p>Here is a global figure that summarizes all these operations, and thus the construction of a Convolutional Kernel Layer.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog_ckn/CKN-480.webp 480w,/assets/img/blog_ckn/CKN-800.webp 800w,/assets/img/blog_ckn/CKN-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog_ckn/CKN.png" class="img-fluid rounded z-depth-0" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>After these operations, we have constructed a “feature map” \(I_1 : \Omega_1 \to \mathbb{R}^{p_1}\), that can be re-used. We can now build a <strong>Convolutional Kernel Network</strong> by stacking Convolutional Kernel Layers, and we will have a <em>representation</em> of the image at the output.</p> <h2 id="cnn-vs-ckn-️">CNN vs. CKN ⚔️</h2> <h3 id="overview">Overview</h3> <p>In <d-cite key="bietti2018group"></d-cite>, it is shown that CKNs contain a large class of CNNs with smooth homogeneous activation functions.</p> <figure> <picture> <source class="responsive-img-srcset" srcset="/assets/img/blog_ckn/CNN-480.webp 480w,/assets/img/blog_ckn/CNN-800.webp 800w,/assets/img/blog_ckn/CNN-1400.webp 1400w," sizes="95vw" type="image/webp"/> <img src="/assets/img/blog_ckn/CNN.png" class="img-fluid rounded z-depth-0" width="100%" height="auto" data-zoomable="" loading="lazy" onerror="this.onerror=null; $('.responsive-img-srcset').remove();"/> </picture> </figure> <p>The similarities and differences between CKN and CNN are well illustrated in the two previous figures.</p> <p>On the one hand, A CNN of \(K\) layer can be represented by its output \(f_{\text{CNN}}(\mathbf{x})\), if \(\mathbf{x}\) is the input, as :</p> \[\begin{equation*} f_{\text{CNN}}(\mathbf{x}) = \gamma_K(\sigma_K(W_K\dots \gamma_2(\sigma_2(W_2\gamma_1(\sigma_1(W_1\mathbf{x}))\dots)) \end{equation*}\] <p>where \((W_k)_k\) represent the convolution operations, \((\sigma_k)_k\) are pointwise non-linear functions, (e.g., ReLU), and \((\gamma_k)_k\) represent the pooling operations (see <d-cite key="paulin2016convolutional"></d-cite>).</p> <p>On the other hand, A CKN of \(K\) layer can be represented by its output \(f_{\text{CKN}}(\mathbf{x})\), if \(\mathbf{x}\) is the input, as :</p> \[\begin{equation*} f_{\text{CKN}}(\mathbf{x}) = \gamma_K(\sigma_K(W_K(P_K\dots \gamma_2(\sigma_2(W_2(P_2(\gamma_1(\sigma_1(W_1(P_1(\mathbf{x}))\dots)) \end{equation*}\] <p>where \((P_k)_k\) represent the patch extractions, \((W_k)_k\) the convolution operations, \((\sigma_k)_k\) the kernel operations (which allows us to learn non-linearity in the RKHS), and \((\gamma_k)_k\) the pooling operations.</p> <h2 id="getting-your-hands-dirty-️">Getting your hands dirty 🖥️</h2> <p>We provide an open-source implementation of the CKN architecture in pure numpy <a href="https://github.com/ozekri/CKN_from_Scratch">here</a> to better understand how things work without having to read pages of documentation of modern deep learning framework such as <code class="language-plaintext highlighter-rouge">PyTorch</code>, <code class="language-plaintext highlighter-rouge">TensorFlow</code> or <code class="language-plaintext highlighter-rouge">JAX</code>. For practical usage, the original implementation can be found <a href="https://github.com/claying/CKN-Pytorch-image">here</a>.</p> <h3 id="stonage-ml">Stonage ML</h3> <h3 id="challenges">Challenges</h3> <h4 id="autodiff">Autodiff</h4> <h4 id="convolutional-operations">Convolutional operations</h4> <p>Little-known fact: <a href="https://pytorch.org/docs/stable/generated/torch.nn.Conv2d.html"><code class="language-plaintext highlighter-rouge">torch.conv2D</code></a> computes a <a href="https://en.wikipedia.org/wiki/Cross-correlation">cross-correlation</a>, not a <a href="https://en.wikipedia.org/wiki/Convolution">convolution</a> (yeah, the name is confusing! :weary:). But what is the difference ? The discrete convolution operator \(\ast\) between two functions \(g\) and \(h\) is defined as</p> \[\begin{equation*} (g \ast h)[n]=\sum_{m=-\infty}^{\infty} g[m] h[n-m] \end{equation*}\] <p>whereas the discrete cross-correlation operator \(\circledast\) is defined as</p> \[\begin{equation*} (g \circledast h)[n]=\sum_{m=-\infty}^{\infty} \overline{g[m]} h[n+m] \end{equation*}\] <p>where \(\overline{g[m]}\) denotes the complex conjugate of \(g[m]\). It’s subtle, but in the case of images, cross-correlation requires one less “image flipping” than convolution because of the minus sign in \(h[n-m]\). Given the number of convolutions we’re going to calculate, if we can spare ourselves an “image flip” each time, we’ll do it! What’s more, as the filter parameters are learnable, it doesn’t matter if we choose cross-correlation rather than convolution.</p> <p>That being said, let’s underline the fact that accelerating the correlation operations is crucial, as it is very much part of the framework. In fact, we use correlations for convolutional layers, but we’ve also implemented linear pooling as a correlation ! These operations will be performed so frequently that, with a from scratch implementation, it’s absolutely essential to parallelize the process, to make it run much faster on GPUs.</p> <h4 id="parallelization-️">Parallelization ⛷️</h4> <p>We’re not allowed to use <a href="https://pytorch.org/docs/stable/generated/torch.nn.functional.conv2d.html"><code class="language-plaintext highlighter-rouge">torch.nn.functional.conv2d</code></a>, so to parallelize <a href="https://docs.scipy.org/doc/scipy/reference/generated/scipy.signal.correlate2d.html#scipy.signal.correlate2d"><code class="language-plaintext highlighter-rouge">scipy.signal.correlate2d</code></a>, we’re left with two solutions.</p> <h5 id="use-cupy-">Use CuPy !</h5> <p>[<code class="language-plaintext highlighter-rouge">CuPy</code>] is an open source library for GPU-accelerated computing with Python programming language. <code class="language-plaintext highlighter-rouge">CuPy</code> shares the same API set as <code class="language-plaintext highlighter-rouge">NumPy</code> and <code class="language-plaintext highlighter-rouge">SciPy</code>, allowing it to be a drop-in replacement to run NumPy/SciPy code on GPU.</p> <p>On top of that, <code class="language-plaintext highlighter-rouge">CuPy</code> has its own implementation of <code class="language-plaintext highlighter-rouge">scipy.signal.correlate2d</code> <a href="https://docs.cupy.dev/en/latest/reference/generated/cupyx.scipy.signal.correlate2d.html">here</a>, and it performs superbly. See the code below for comparision with the original one, and <code class="language-plaintext highlighter-rouge">torch.nn.functional.conv2d</code>.</p> <details> <summary>Code</summary> <pre><code class="language-python">import timeit
        import numpy as np
        import cupy as cp
        from scipy import signal
        from utils import torch_correlate2d
        from cupyx.scipy.signal import correlate2d as cupyx_correlate2d
        
        # Creating a test image and kernel
        image = np.random.rand(5000, 5000)
        kernel = np.random.rand(10, 10)
        
        # Correlation calculation with scipy.signal.correlate2d
        start_time = timeit.default_timer()
        result_scipy = signal.correlate2d(image, kernel, mode='valid')
        scipy_time = timeit.default_timer() - start_time
        print("Execution time with scipy.signal.correlate2d :", scipy_time)
        
        # Correlation calculation with torch.nn.functional.conv2d
        start_time = timeit.default_timer()
        result_torch = torch_correlate2d(image, kernel, padding='valid')
        torch_time = timeit.default_timer() - start_time
        print("Execution time with torch.nn.functional.conv2d :", torch_time)
        
        # Correlation calculation with cupyx.scipy.signal.correlate2d
        image_gpu = cp.asarray(image)
        kernel_gpu = cp.asarray(kernel)
        start_time = timeit.default_timer()
        result_cupyx = cupyx_correlate2d(image_gpu, kernel_gpu, mode='valid')
        cupyx_time = timeit.default_timer() - start_time
        print("Execution time with cupyx.scipy.signal.correlate2d :", cupyx_time)</code></pre></details> <p>Note that there also exist a <a href="https://jax.readthedocs.io/en/latest/_autosummary/jax.scipy.signal.correlate2d.html">LAX-backend implementation</a> of <code class="language-plaintext highlighter-rouge">scipy.signal.correlate2d</code> in <code class="language-plaintext highlighter-rouge">JAX</code>.</p> <h5 id="use-a-low-level-language">Use a Low-Level Language</h5> <p>You can also mplement the function in a low-level language such as C or C++ for better performance, and use a high-level language like Python to call this implementation.</p> <p><a id="acknowledgments"></a></p> <h2 id="acknowledgments-">Acknowledgments 🙏🏾</h2> <p>For any further questions, please feel free to leave a comment or contact the authors by mail!</p>]]></content><author><name>Oussama Zekri</name></author><category term="kernel trick"/><category term="convolutional networks"/><category term="deep learning"/><category term="maths"/><category term="code"/><summary type="html"><![CDATA[Convolutional Kernel Networks]]></summary></entry></feed>